# Session Note - 2026-05-06

## Done
- Added `missed_swing_type` to the trade model, settle request schema, database DDL, and trade write/read mapping.
- Stored and returned the new type through the settle and resettle API paths.
- Updated the settlement page to track the new type, prefill it in edit mode, and send it in the POST payload.
- Replaced the yes/no missed-opportunity toggle with a 3-way choice: profit, scratch, or none.
- Split the simulator UI into a smaller shared component and kept the notes file under the component size limit.

## Decisions
- Kept the new type as a nullable string in the backend and a UI state value that only sends `PROFIT`, `SCRATCH`, or `null`.
- Hid the simulator when the current trade book is green, while leaving the rest of the notes form visible.
- Reused the existing missed-swing math and fields without changing the calculation itself.

## Checks
- `python core/utils/python_type_check.py --root .` passed.
- `cd frontend; npx tsc --noEmit` passed.
- Targeted `eslint` on the touched frontend files passed.
- `python core/utils/paradigm_sentinel.py --root .` passed.
- `python core/utils/compliance_bouncer.py --root .` passed.
- Repo-wide frontend lint/paradigm sentinels still report pre-existing unrelated violations in other files.

## Next
- If needed, I can clean up the remaining repo-wide frontend sentinel violations, but they are outside this trade-settlement change.

## Bet Fix
## Done
- Rebuilt stale `bets` tables during `migrate_trades_db` so a leftover `trades_legacy` foreign key is repaired automatically.
- Added a regression test that recreates the broken bets foreign key and confirms the migration keeps existing bet rows.
- Repaired `data/ipl_master.sqlite` in place and verified a live bet POST now succeeds again.

## Decisions
- Kept the fix in the migration layer so the app heals itself on startup instead of needing a manual database wipe.
- Preserved existing bet rows during the rebuild.

## Checks
- `python -m pytest tests/test_cockpit_api.py -k "migrate_trades_db_rebuilds_stale_relational_schema or migrate_trades_db_rebuilds_stale_bets_foreign_key or migrate_trades_db_recovers_from_stale_trades_legacy_table or bet_routes_support_crud_and_validation or bet_delete_removes_one_bet_and_resets_trade_state or cashout_bets_skip_bankroll_guard" -q --tb=short` passed.
- `python -m pytest tests/test_cockpit_history_api.py -q --tb=short` passed.
- `python core/utils/paradigm_sentinel.py` passed.
- `python core/utils/compliance_bouncer.py` passed.
- `python -m ruff check cockpit/database.py tests/test_cockpit_api.py` could not run because `ruff` is not installed here.
- `python -m mypy cockpit/database.py tests/test_cockpit_api.py` reported pre-existing repo-wide type errors unrelated to this change.

## Next
- Restarting the backend is still a good idea so the running process loads the patched code, although the live database file is already repaired.

## Toast Fix
## Done
- Moved the live bet success/error popup out of the top-right wallet area and into a bottom-right toast.
- Added a slide-in-from-right animation and a cleaner glass-style surface for the toast.
- Split the toast logic into a small reusable hook and a small toast view so `LiveTradeExecutionPanel.tsx` stays under the 300-line warning limit.

## Decisions
- Kept the change scoped to the live execution panel that raises the "Bet placed." toast.
- Left the undo snackbar alone so the bet-placement fix does not change other notifications.
- Used the existing 3-second dismiss timing from the panel behavior.

## Checks
- `Set-Location 'C:\Cricket_Project_Stable\frontend'; .\node_modules\.bin\eslint.cmd .\components\cockpit\LiveTradeExecutionPanel.tsx .\components\cockpit\LiveTradeToast.tsx .\lib\cockpit\useLiveTradeToast.ts` passed.
- `Set-Location 'C:\Cricket_Project_Stable\frontend'; .\node_modules\.bin\tsc.cmd --noEmit` passed.
- `LiveTradeExecutionPanel.tsx` is 267 lines.
- `LiveTradeToast.tsx` is 17 lines.
- `python core/utils/compliance_bouncer.py --root .` passed.
- `python core/utils/paradigm_sentinel.py --root .` passed.
- `python core/gen_ai/skills/validators/frontend/frontend-paradigm-sentinel/scripts/run_frontend_paradigm.py --root .` still reports three pre-existing oversized cockpit files: `frontend/components/cockpit/cockpit-api.ts`, `frontend/components/cockpit/CockpitMatchSetup.tsx`, and `frontend/components/cockpit/HistoryFiltersModal.tsx`.

## Next
- If you want, I can clean up the remaining cockpit files that still trip the repo-wide frontend line-count gate.
