# Session Notes - 2026-05-06

## What I changed
- Fixed the missed-opportunity simulation math in `frontend/lib/cockpit/live-trade-calcs.ts`.
- Updated `frontend/components/cockpit/SettlementMissedOpportunitySimulator.tsx` to show the signed actual loss and the new bottom-line delta.

## What I decided
- Kept the existing result shape so the save/resettle flow stays compatible.
- Used the selected team as the target team and treated the other side as the opposing team when building the book position.
- Kept the backend payload field `missed_swing_net_pnl` as the simulated cashout PnL, not the bottom-line delta.

## Verification
- `tsc` passed for the frontend project.
- Frontend lint/paradigm validation still fails in unrelated existing files, not in the files I changed.

## Next
- If needed, split the unrelated frontend validator failures in the existing cockpit and history files.

## Added Later
- Rewrote `agents/plans/V2_DATABASE_NORMALIZATION_PLAN.md` to make the normalization plan more accurate and more detailed.
- Added the real cockpit frontend files that still reference `TradeResponse`, including `frontend/components/cockpit/cockpit-api.ts`, `HistoryDashboard.tsx`, `HistoryTradeTable.tsx`, `HistoryBankrollChart.tsx`, `history-dashboard-utils.ts`, `live-trade-bets-api.ts`, and `useLiveTradeBets.ts`.
- Clarified the backend rebuild order: SQLite schema and migration first, Python models and schemas second, frontend contract and UI third, then tests and gates.
- Corrected the plan so it explicitly removes the stale `is_fake_favourite` filter from the frontend contract if the backend no longer supports it.
- Created four separate sub-plan files under `agents/plans/sub_plans/` for the migration, backend contract, frontend cleanup, and verification steps.
- Implemented Sub-Plan 1 in `cockpit/database.py` by removing the flattened bullet columns and dead flags from the SQLite trade DDL, expanding the rebuild trigger column checks, and keeping the legacy rebuild path intact.
- Strengthened `tests/test_cockpit_api.py` so the migration tests now assert the removed bullet columns and dead flags are absent after rebuild.
- Verified the migration-focused cockpit tests with `python -m pytest tests/test_cockpit_api.py -k migrate_trades_db -x -q --tb=short` and they passed.
- Implemented Sub-Plan 2 in `cockpit/models.py`, `cockpit/schemas.py`, `cockpit/database.py`, and `tests/test_cockpit_api.py` by removing the stale trade response fields, trimming the write column list, cleaning the legacy import path, and adding contract tests that reject removed fields.
- Verified the touched cockpit contract tests with `python -m pytest tests/test_cockpit_api.py -k \"trade_creation_reuses_the_same_match_row or trade_requests_reject_removed_fields or routes_support_crud or trade_list_filters_by_match_season or trade_settlement_records_metrics_and_locks_trade or trade_restore_recreates_a_settled_trade_without_recrediting_wallet\" -x -q --tb=short` and they passed.
- Implemented Sub-Plan 3 in `frontend/components/cockpit/cockpit-api.ts` and `frontend/lib/cockpit/history-api.ts` by removing the stale `bullet_*`, `fav_reached_130`, and `is_fake_favourite` fields from the frontend trade contracts.
- Confirmed there were no remaining frontend references to the removed fields after the patch.
- Verified the modified frontend files with `.\node_modules\.bin\eslint.cmd components/cockpit/cockpit-api.ts lib/cockpit/history-api.ts` and `.\node_modules\.bin\tsc.cmd --noEmit`, and both passed.
- Implemented the Sub-Plan 1 rebuild fix in `cockpit/database.py` so flat legacy trade tables without `match_id` are rebuilt into an empty normalized table instead of crashing on `NOT NULL`.
- Re-ran `python -m pytest tests/test_cockpit_api.py -x -q --tb=short`, `python core/utils/python_type_check.py --root .`, `python core/utils/paradigm_sentinel.py --root .`, and `python core/utils/compliance_bouncer.py --root .`; all of those passed after the migration fix.
- Re-ran the frontend verification checks: `.\node_modules\.bin\eslint.cmd components/cockpit/cockpit-api.ts lib/cockpit/history-api.ts`, `.\node_modules\.bin\tsc.cmd --noEmit`, and `python core/gen_ai/skills/validators/frontend/frontend-type-sync-guard/scripts/run_type_sync.py --root .` passed.
- The repo-wide frontend sentinels still report many pre-existing violations in unrelated files (`frontend-lint-sentinel` and `frontend-paradigm-sentinel`), so those are not caused by this sub-task and were not expanded here.
