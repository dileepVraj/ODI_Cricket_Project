# Session Note - 2026-04-28

What was done:
- Fixed the live Book card value wrapping in `frontend/components/cockpit/LiveTradeBookPanel.tsx` by making the amount line stay on one line and truncate cleanly instead of wrapping.
- Added a tooltip on the displayed book value so the full amount is still visible on hover.
- Brightened the team accent colors for DC, LSG, MI, and the rest of the current team map in `frontend/lib/cockpit/team-colors.ts`.
- Moved the actual team color hex values into `frontend/app/globals.css` so the helper file stays lint-friendly.
- Reduced the Book panel number size again so more of the profit/loss amount fits inside the card without clipping.
- Kept the change scoped to the cockpit UI and shared color tokens.

What was decided:
- Use CSS-driven truncation for the book value instead of letting the Rs prefix and the number split across lines.
- Use bright, high-contrast team colors for dark mode instead of deep or muted shades.
- Leave unrelated repo-wide lint and typecheck problems untouched for now.

What comes next:
- If needed, clean up the pre-existing frontend lint and TypeScript issues that are blocking the project gates.
- Re-check the live trading screen in the browser to confirm the book cards now stay aligned on narrow widths.

## Break-even math update

What was done:
- Replaced the Strategy HUD break-even logic in `frontend/lib/cockpit/live-trade-strategy.ts` with the exchange ratio formula based on the profitable team PnL and the losing team PnL.
- Changed the return shape to use `teamWithProfit` so the HUD shows the team that must be laid.
- Updated `frontend/components/cockpit/LiveTradeStrategyHUD.tsx` to read the new return field.

What was decided:
- Keep the result as a direct ratio calculation with no 99p cap.
- Treat the trade as risk-free only when both sides are already non-negative or when the ratio would otherwise be undefined.

What comes next:
- If the project wants the repo-wide frontend gates green, the unrelated existing lint and TypeScript violations need separate cleanup.

## Live trade ledger update

What was done:
- Added `frontend/components/cockpit/LiveTradeLedger.tsx` as a compact dark table for live bet history.
- Wired `frontend/app/(shell)/trading-dashboard/[tradeId]/page.tsx` to the existing bet hook so the ledger loads the current trade's bets and refreshes after each successful bet.
- Placed the ledger directly below the Live Execution panel with a scrollable max-height container and a sticky header.

What was decided:
- Sort bets newest-first by timestamp, with `id` as the tie-breaker.
- Show a loading state while the bet list is being fetched, and only show the empty state once loading has finished.
- Keep the team color display driven by `getTeamColor()` and use badges for BACK and LAY.

What was verified:
- `eslint` passed for the two changed TypeScript files.
- Full `tsc --noEmit` still fails because of pre-existing unrelated TypeScript errors in `frontend/components/cockpit/HistoryTradeRow.tsx`, `HistoryView.tsx`, and `HistoryViewSections.tsx`.

What comes next:
- If needed, clean up the older cockpit type drift so the repo-wide TypeScript check can pass again.

## Trade settlement lifecycle

What was done:
- Added settlement columns to the Cockpit trades schema in `cockpit/database.py`, including sentiment, favorite-under-30 loss, targeted PnL, achieved yield, volume wagered, and settled status support.
- Added the `POST /api/cockpit/trades/{id}/settle` endpoint in `api/cockpit/router.py` to lock active trades, calculate realized PnL from the current book state, and store the journaling fields.
- Wired the live trading UI to show a `Settle Match` button, open a settlement modal, and pass the official result plus journaling data back to the API.
- Made the live execution panel and cash-out controls read-only once a trade is settled.
- Added settled-state badges and summary values in the live trade hero strip.

What was decided:
- Store the final winner separately from the journaling sentiment so the result and the operator notes do not get mixed together.
- Use the current bet book to calculate realized PnL and total volume at settlement time instead of trusting the UI to send those numbers.
- Hide the settlement button and disable execution controls entirely after the trade is locked.

What was verified:
- `python -m pytest tests/test_cockpit_api.py -x -q --tb=short` passed.
- `eslint` passed for the changed frontend files.
- `tsc --noEmit` passed for the frontend project.
- `ruff check` passed for the changed Python files.
- `mypy` still reports many pre-existing repo-wide issues, mostly missing pandas stubs and older type drift outside this change.

What comes next:
- If the repo wants the full Python type check green, the existing pandas stub and legacy typing issues need a separate cleanup.
- The settled trade flow can now be exercised in the browser on the live trading page.

## Trade draft teardown

What was done:
- Added `resetDraftState` to the shell-mounted `CockpitTradeDraftContext` so the draft can be fully cleared, including `selectedTradeId`.
- Updated `frontend/app/(shell)/trading-dashboard/page.tsx` so `?action=new` clears the draft on mount and then strips the query param back out of the URL.
- Changed the sidebar Trading button in `frontend/components/layout/Sidebar.tsx` to land on the setup page with `action=new` so an explicit new setup starts from a blank draft.
- Hooked `frontend/components/cockpit/LiveTradeSettlementModal.tsx` into the cockpit draft context so a successful settlement immediately purges the draft memory.
- Added a backend guard in `api/cockpit/router.py` so `PATCH /api/cockpit/trades/{id}` rejects settled trades with a 400.
- Added a regression test in `tests/test_cockpit_api.py` for the new settled-trade update guard.

What was decided:
- Keep the draft reset centralized in the shell context instead of spreading cleanup logic across multiple components.
- Use the URL action flag only as a one-time trigger for a new setup, then remove it so it does not keep re-firing.
- Let the backend enforce update safety even if the frontend accidentally reuses an old trade id.

What was verified:
- `python -m pytest tests/test_cockpit_api.py -x -q --tb=short` passed.
- `python core/utils/python_type_check.py --root .` passed.
- `node C:\\Cricket_Project_Stable\\frontend\\node_modules\\typescript\\bin\\tsc --noEmit -p C:\\Cricket_Project_Stable\\frontend\\tsconfig.json` passed.
- `python core/gen_ai/skills/validators/frontend/frontend-paradigm-sentinel/scripts/run_frontend_paradigm.py --root .` passed.
- `python core/utils/paradigm_sentinel.py --root .` still fails because the repo has unrelated existing bouncer and SRP violations.
- `python core/utils/compliance_bouncer.py --root .` still fails with the same pre-existing repo-wide violations.

What comes next:
- If needed, clean up the unrelated repo-wide compliance violations that are still blocking GATE5P and GATE6.
- Re-test the live trading setup flow in the browser to confirm a new setup no longer inherits the old trade id.

## Zombie state follow-up

What was done:
- Tightened `frontend/app/(shell)/trading-dashboard/page.tsx` so the draft resets on setup-page entry by default, not only when `action=new` is present.
- Kept the URL cleanup for `action=new` so the one-time "new setup" trigger still clears the query string after it runs.

What was decided:
- Treat the setup page as a blank slate on entry to stop stale trade ids from surviving browser back/forward navigation.

What was verified:
- `node C:\\Cricket_Project_Stable\\frontend\\node_modules\\typescript\\bin\\tsc --noEmit -p C:\\Cricket_Project_Stable\\frontend\\tsconfig.json` passed again.
- `eslint` still reports only the existing Next.js pages-directory warning.

What comes next:
- Check the live trade execute flow again in the browser after this broader reset change.
