# Session Note - 2026-04-29

## Done
- Diagnosed the live trade ledger bleed as frontend state retention in `useLiveTradeBets`.
- Confirmed the backend bets endpoint is correctly scoped to `trade_id`.
- Updated the trade page to force a remount on trade changes and to await bet refreshes in sequence.
- Added a hook-level bets reset before refetch and on cleanup.

## Decided
- The clean fix is defensive on the frontend, not a backend query change.
- The bets hook now owns the wipe behavior so stale rows cannot survive a route switch.
- The page now waits for POST, trade refresh, and bet refresh before the submit flow finishes.

## Checks
- Frontend lint sentinel: failed on many pre-existing repo-wide violations outside these files.
- Frontend paradigm sentinel: passed.
- TypeScript check: passed.
- Compliance bouncer: failed on pre-existing repo-wide violations.

## Next
- No code change is needed for the leakage bug itself.
- If requested, the next step would be to clean up the repo-wide compliance failures that still block the final gates.

## Open Positions Fix
- Found that the cockpit API was re-running database initialization on every request, which let the open-trades panel hit intermittent 500s while the page fired several requests at once.
- Added a one-time initialization guard in `cockpit/database.py` and warmed both cockpit formats during API startup in `api/main.py`.
- Verified with a fresh FastAPI `TestClient` run that `/api/cockpit/trades?format=ipl&status=ACTIVE`, `/api/cockpit/teams?format=ipl`, `/api/cockpit/venues?format=ipl`, and `/api/cockpit/trades?format=ipl&status=DRAFT` all return 200 together.
- Reloaded the live dashboard browser tab after restarting the API server and confirmed the open-positions request now returns 200 with no current error banner.

## Live Trade Ledger Fix
- Found that trade 49 had two stale PBKS bet rows in the database, even though the live trade is MI vs SRH.
- Added a backend live-view filter in `api/cockpit/router.py` so the ledger and trade book only use bets whose team still matches the current trade teams.
- Restarted the API process and confirmed `GET /api/cockpit/trades/49/bets?format=ipl` now returns `[]`, while `GET /api/cockpit/trades/49/state?format=ipl` still reports the correct MI vs SRH book state.
- Reloaded the browser page and confirmed the Trade Ledger now shows `0 bets` instead of the old PBKS rows.

## Trade Mapping Tightening
- Removed the live/history time-cutoff heuristic and made the bets API rely on `trade_id` only for ownership.
- Added a regression test that creates two separate active trades with the same teams and confirms each bet list stays isolated to its own `trade_id`.
- Fixed the Cockpit DB init cache so it re-initializes when the temp test database paths change or are deleted between tests.
- Switched the IPL lookup connection to read-only so write routes only lock the trades SQLite file, not the static lookup DuckDB.
- Updated the Cockpit API test fixture to use temporary copies of both the IPL lookup DuckDB and the trades SQLite file.
- Verified `python -m pytest tests/test_cockpit_api.py -q` passes with 11 tests green.
