# Session Note - 2026-05-05

## What I changed
- Added a backend restore endpoint that rebuilds a deleted trade snapshot without replaying wallet credits.
- Switched the frontend restore flow to call that new endpoint instead of create + settle.
- Added a regression test that proves restoring a settled trade does not change the wallet balance.
- Repaired `data/finances.sqlite` by removing duplicate trade stake and payout rows and recalculating the account balances.
- Moved the test suite onto isolated temp finance files so test runs no longer write into the live money ledger.
- Added app shutdown cleanup so the temp finance files close cleanly on Windows before deletion.
- Seeded the temp finance files with a safe starting bankroll so cockpit trade tests can still create trades.
- Removed the extra payout rows created by my test runs from the live finance ledger again after the isolation patch.

## Files modified
- `api/cockpit/router.py`
- `api/main.py`
- `cockpit/schemas.py`
- `frontend/components/cockpit/cockpit-api.ts`
- `frontend/lib/cockpit/trade-restore.ts`
- `tests/test_cockpit_api.py`
- `tests/test_cockpit_history_api.py`
- `tests/test_api_integration.py`
- `data/finances.sqlite`

## What I decided
- The root bug was the undo/restore flow replaying a normal settlement, which created a second wallet credit.
- Restore now rehydrates the saved trade and bets directly, so it does not touch finances at all.
- I also cleaned the current finance ledger so the wallet matches the corrected history.

## Checks
- `python -m pytest tests/test_cockpit_api.py -q --tb=short` PASS
- `python -m pytest tests/test_cockpit_history_api.py -q --tb=short` PASS
- `python -m pytest tests/test_cockpit_api.py tests/test_cockpit_history_api.py tests/test_api_integration.py -q --tb=short` PASS
- `Set-Location 'frontend'; .\\node_modules\\.bin\\eslint.cmd components\\cockpit\\cockpit-api.ts lib\\cockpit\\trade-restore.ts` PASS
- `Set-Location 'frontend'; .\\node_modules\\.bin\\tsc.cmd --noEmit` PASS
- `python core\\utils\\paradigm_sentinel.py` PASS
- `python core\\utils\\compliance_bouncer.py` PASS
- `python core\\utils\\frontend_paradigm_sentinel.py` FAIL because of pre-existing oversized components outside this change
- `python -m ruff check ...` could not run because `ruff` is not installed in this Python environment
- `python -m mypy ...` failed with pre-existing repo type issues unrelated to this change

## What comes next
- If needed, I can tackle the pre-existing frontend size violations flagged by the paradigm sentinel.

## Task: bankroll chart chronology fix
**Status:** DONE
**What:** Changed the bankroll timeline to sort by `match_date` ascending with `id` as the tie-breaker, so the chart follows the real match order.
**Commit:** BLOCKED by the repository scope guard for `SRP-ADVISORY-FIX`.
**Gate state:** ESLint PASS, TypeScript PASS.
**Next:** If the task scope is expanded, I can commit this change cleanly.
